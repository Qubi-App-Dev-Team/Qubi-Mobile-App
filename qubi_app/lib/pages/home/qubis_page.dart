import 'package:flutter/material.dart';

class QubisPage extends StatelessWidget {
  const QubisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Qubis Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}